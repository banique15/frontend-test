import { map, atom } from 'nanostores';

export const camResponses = map({'success':false,'message':''});
export const subResponses = map({'success':false,'message':''});
export const audienceSelected = atom('6fbabdedf8');